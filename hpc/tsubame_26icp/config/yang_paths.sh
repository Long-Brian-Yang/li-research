#!/usr/bin/env bash
# Canonical TSUBAME paths for the uf03782/tgj-26ICP workspace.
export YANG_ROOT="${YANG_ROOT:-/gs/fs/tgj-26ICP/uf03782/yang}"
export PROJECT_ROOT="${TSUBAME_TEST_ROOT:-$YANG_ROOT/li-research}"
export ENV_ROOT="${ENV_ROOT:-$YANG_ROOT/envs}"
export DOCS_ROOT="${DOCS_ROOT:-$PROJECT_ROOT/docs}"
export HPC_ROOT="${HPC_ROOT:-$PROJECT_ROOT/hpc/tsubame_26icp}"
export STRUCTURES_ROOT="${STRUCTURES_ROOT:-$PROJECT_ROOT/structures}"
export MODELS_ROOT="${MODELS_ROOT:-$PROJECT_ROOT/models}"
export ENGINES_ROOT="${ENGINES_ROOT:-$PROJECT_ROOT/engines}"
export BENCHMARKS_ROOT="${BENCHMARKS_ROOT:-$PROJECT_ROOT/benchmarks}"
export RUNS_ROOT="${RUNS_ROOT:-$PROJECT_ROOT/runs}"
export RESULTS_ROOT="${RESULTS_ROOT:-$PROJECT_ROOT/results}"
export MACE_ENV="${MACE_PYTHON_ENV:-$YANG_ROOT/envs/mace_env}"
export SEVENNET_ENV="${SEVENNET_PYTHON_ENV:-$YANG_ROOT/envs/sevennet_env}"
export MATGL_ENV="${MATGL_PYTHON_ENV:-$YANG_ROOT/envs/matgl_env}"
export MACE_LMP_MLIAP="${MACE_LMP_MLIAP:-$ENGINES_ROOT/lammps/mace/install_mliap_gpu/bin/lmp}"
export SEVENNET_LMP="${SEVENNET_LMP:-$ENGINES_ROOT/lammps/sevennet/install/bin/lmp}"
export MATGL_LMP="${MATGL_LMP:-$ENGINES_ROOT/lammps/matgl/install/bin/lmp}"
export MATGL_SOURCE_ROOT="${MATGL_SOURCE_ROOT:-$ENGINES_ROOT/matgl/source}"
export MATGL_RUNTIME_LIBS="${MATGL_RUNTIME_LIBS:-$ENGINES_ROOT/matgl/runtime-libs}"
export MATGL_LAMMPS_BUILD_ROOT="${MATGL_LAMMPS_BUILD_ROOT:-$ENGINES_ROOT/lammps/matgl/develop/build-matgl-gpu}"
export GPUMD_ROOT="${GPUMD_ROOT:-$ENGINES_ROOT/gpumd/source}"
export GPUMDKIT_ROOT="${GPUMDKIT_ROOT:-$ENGINES_ROOT/gpumdkit/source}"
export GPUMD_ENGINE="${GPUMD_ENGINE:-$GPUMD_ROOT/src/gpumd}"
export NEP89_MODEL="${NEP89_MODEL:-$MODELS_ROOT/nep89/nep/nep89_20250409/nep89_20250409.txt}"
