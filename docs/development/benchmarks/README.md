# Benchmarks

This section contains one current benchmark matrix. It must only use runs with
a recorded common protocol; obsolete retest notes are merged into the matrix or
removed rather than retained as parallel documentation.
